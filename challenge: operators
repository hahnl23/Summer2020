//Psuedocode:
// Declare scanner
//Input the cost of the meal before tax and tip (mealCost) with system.print and scanner
//Input the percentage of cost of meal being added as tip (tipPercent) with system.print and scanner
//Input the percentage of cost of meal being added as tax (taxpercent) with system.print and scanner
//Declare variables for tip, tax, and totalCost
//Make equations for tip and tax
//Make equation for totalCost
//Print totalCost

package com.company;
import java.util.Scanner;
public class operatorschallenge {

    public static void main (String [] args){

        Scanner Console = new Scanner(System.in);
        System.out.println("The cost of the meal before tax and tip: ");
        double mealCost = Console.nextDouble();

        Console = new Scanner(System.in);
        System.out.println("The percentage of the cost of the meal being added as tip: ");
        int tipPercent = Console.nextInt();

        Console = new Scanner(System.in);
        System.out.println("The percentage of the cost of the meal being added as tax: ");
        int taxPercent = Console.nextInt();

        int tip;
        int tax;
        double totalCost;

        tip = (int) (mealCost * 20/100);
        tax = (int) (mealCost * 8/100);

        totalCost = mealCost + tip + tax;
        Math.round(totalCost);


        System.out.println("Your total cost is: " + totalCost);
    }


}
